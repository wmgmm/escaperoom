// Shared leaderboard config.
// After deploying the Google Apps Script (see google_apps_script.js in project root):
//   1. Set LEADERBOARD_URL to your Web App deployment URL
//   2. Set LEADERBOARD_TOKEN to the same secret string used in the script
// Leave LEADERBOARD_URL as '' to run in local-only mode (safe for dev/testing).

export const LEADERBOARD_URL = '';
export const LEADERBOARD_TOKEN = '';
